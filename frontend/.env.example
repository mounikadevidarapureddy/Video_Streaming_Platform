# Leave empty when frontend and backend share the same deployment origin.
VITE_API_URL=/api

# For separate deployments, use the public backend URL, for example:
# VITE_API_URL=https://your-backend.example.com/api
